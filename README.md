# Minimal PTO Planner

A dependency-free, year-at-a-glance PTO planner designed for static hosting.

## Run locally

```bash
python3 -m http.server 4173
```

Then open <http://localhost:4173>.

Planner data is stored only in the current browser's `localStorage`.
